server {
    listen 80;
    server_name tricolor-austral.duckdns.org;

    location / {
        root /usr/share/nginx/html;
    }

    location /.well-known/acme-challenge/ {
        root /var/www/html;
        allow all;
    }
}